const corsProxy = ""    // for using bnnbloomberg-markets-scraper in a browser, you must pass your requests through a CORS proxy. See https://github.com/Rob--W/cors-anywhere

module.exports = {
    corsProxy
}